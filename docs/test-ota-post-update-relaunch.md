# TEST ProDVX OTA relaunch finding (2026-09-20)

Hardware: ProDVX ABPC-4220, Android 13. User installed 0.5.16-test through OTA and reports that the app did not automatically return to foreground; manually started it. Read-only TEST DB check at 20:17 UTC: device `gang` active, reporting `0.5.16-test`, fresh heartbeat and sync, config revision 3 applied. Thus this is a relaunch failure, not proof of failed APK installation.

Current `BootReceiver` in 0.5.16 receives `MY_PACKAGE_REPLACED` and `BOOT_COMPLETED` and attempts to start `FullscreenActivity`. Android/OEM background activity launch restrictions mean receiver delivery does not guarantee a visible launch. Do not claim this code reliably relaunches on this hardware or blindly add a reboot call.

Physical TEST recovery without app-data reset: Configure ProDVX **Settings > System > Advanced > Digital Signage Options > Auto Start Application > Start App > NarrowVision Player** if firmware exposes these options, then perform one user-approved device reboot and verify automatic login/playlist/heartbeat. Manufacturer documentation: https://www.prodvx.com/onboarding . This configuration addresses boot recovery, but by itself does not guarantee relaunch immediately after a package update.

Engineering requirement for unattended post-OTA recovery: investigate whether a supported ProDVX API/ProMGR service offers authorized reboot or app-foreground actions; alternatively fully managed device-owner/kiosk provisioning with proper app-as-home or management agent. Android `DevicePolicyManager.reboot()` requires device-owner privileges (or privileged policy permission): https://developer.android.com/work/dpc/device-management . Avoid root/shell hacks, unauthorized device reboot, boot loops and provisioning/factory-reset changes on the paired `gang` player. First confirm actual management mode, collect a single TEST `logcat` around `MY_PACKAGE_REPLACED` if accessible, then implement and physically validate on TEST before distributing another APK. No production changes or automatic reboot/OTA dispatch.