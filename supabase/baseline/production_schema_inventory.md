# Supabase baseline inventory

The player-core migration assumes the existing NarrowVision schema provides organizations, organization members, licenses, devices, media items, playlists, playlist items, schedules, support requests and ticket messages, plus the existing manager/customer RPC surface.

Production project `eckmifmgrurxgriimgpp` is a read-only baseline reference. Runtime migration and destructive tests belong exclusively to disposable test project `bqapbwsvfofgnfogwhdx`.
